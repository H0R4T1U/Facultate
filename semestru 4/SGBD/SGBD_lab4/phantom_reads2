-- PROBLEM: SET TRANSACTION ISOLATION LEVEL TO REPEATABLE READ
SET TRANSACTION ISOLATION LEVEL REPEATABLE READ --PROBLEM: LEVEL REPEATABLE READ/Serializiable
BEGIN TRAN
	SELECT * FROM Accounts
	WAITFOR DELAY '00:00:07'
	SELECT * FROM Accounts
COMMIT TRAN